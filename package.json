{
  "name": "konect-api-types-ts",
  "version": "1.0.0",
  "description": "Contains all typescript class and types for the konect App",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "tsc --target ES5 --experimentalDecorators --emitDecoratorMetadata",
    "prepublish": "tsc --target ES5 --experimentalDecorators --emitDecoratorMetadata",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/types": "^7.21.2",
    "@fortawesome/fontawesome-svg-core": "^6.3.0",
    "reflect-metadata": "^0.1.13",
    "uuidv4": "^6.2.13",
    "vue": "^3.2.47"
  },
  "devDependencies": {
    "@types/node": "^18.14.1"
  }
}
